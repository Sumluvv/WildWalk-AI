version: "3.9"
services:
  wildwalk-ai:
    build: .
    container_name: wildwalk-ai
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./.data:/app/.data
    restart: unless-stopped
